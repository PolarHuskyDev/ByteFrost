main(): int {
    x: int = 5;
    y: int = 10;
    if (x > 0) { print("x positive"); }
    if (y > 0) { print("y positive"); }
    if (x > y) { print("x bigger"); }
    if (y > x) { print("y bigger"); }
    return 0;
}
